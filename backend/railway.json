{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "export DATABASE_URL=${DATABASE_PUBLIC_URL:-$DATABASE_URL} && alembic upgrade head && python -m app.seed.run_seed && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
