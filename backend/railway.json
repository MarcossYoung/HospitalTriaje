{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "alembic upgrade head; echo \"Alembic exit: $?\"; python -m app.seed.run_seed; echo \"Seed exit: $?\""
  }
}
